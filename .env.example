# TMDB v4 bearer token — send as `Authorization: Bearer`, NOT as an api_key query param.
TMDB_API_KEY=

DATABASE_URL=postgresql://allblue:allblue@localhost:5432/allblue

# Optional. Watch-history sync only; the app must run fine with these unset.
PLEX_BASE_URL=http://192.168.1.74:32400
PLEX_TOKEN=
