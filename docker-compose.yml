services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: allblue
      POSTGRES_PASSWORD: allblue
      POSTGRES_DB: allblue
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U allblue"]
      interval: 5s
      retries: 10

volumes:
  pgdata:
